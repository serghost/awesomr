<% @user ||= current_user %>
<div class="stats">
  <table summary="User stats">
    <tr>
      <td>
        <a href="<%= following_user_path(@user) %>">
          <span id="following" class="stat">
            <%= @user.following.count %> <%= t("users.title.following") %>
          </span>
        </a>
      </td>
      <td>
        <a href="<%= followers_user_path(@user) %>">
          <span id="followers" class="stat">
            <%= @user.followers.count %> <%= t("users.title.followers") %>
          </span>
        </a>
      </td>
    </tr>
  </table>
</div>
